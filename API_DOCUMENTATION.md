# 📚 توثيق واجهة برمجة التطبيقات (API Documentation)

## نظرة عامة
هذا هو توثيق شامل لجميع نقاط النهاية (API Endpoints) الخاصة بلوحة تحكم تطبيق أبهام للتوصيل.

**عنوان السيرفر الأساسي:** `http://localhost:3000`

---

## 📑 جدول المحتويات
1. [المستخدمين والعملاء](#المستخدمين-والعملاء)
2. [المطاعم](#المطاعم)
3. [الطلبات](#الطلبات)
4. [الكباتن](#الكباتن)
5. [التقارير والإحصائيات](#التقارير-والإحصائيات)
6. [التسويق](#التسويق)
7. [نظام الخطوات](#نظام-الخطوات)
8. [الأوامر](#الأوامر)

---

## المستخدمين والعملاء

### 1. جلب جميع العملاء
**الطريقة:** `GET`
**المسار:** `/api/users/customers`

**الوصف:** يسترجع قائمة بجميع المستخدمين الذين لديهم دور "عميل"

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/users/customers
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "name": "محمد أحمد",
    "email": "mohammed@example.com",
    "phone": "0501234567",
    "role": "customer",
    "status": "active",
    "created_at": "2024-01-15 10:30:00",
    "updated_at": "2024-01-15 10:30:00"
  }
]
```

---

### 2. جلب عميل محدد
**الطريقة:** `GET`
**المسار:** `/api/users/customers/:id`

**المعاملات:**
- `id` (في المسار): رقم معرف العميل

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/users/customers/1
```

**مثال على الاستجابة:**
```json
{
  "id": 1,
  "name": "محمد أحمد",
  "email": "mohammed@example.com",
  "phone": "0501234567",
  "role": "customer",
  "status": "active",
  "created_at": "2024-01-15 10:30:00",
  "updated_at": "2024-01-15 10:30:00"
}
```

---

### 3. تحديث بيانات عميل
**الطريقة:** `PUT`
**المسار:** `/api/users/customers/:id`

**المعاملات:**
- `id` (في المسار): رقم معرف العميل

**البيانات المطلوبة في الـ Body:**
```json
{
  "name": "محمد أحمد المحدث",
  "email": "newemail@example.com",
  "phone": "0509876543"
}
```

**مثال على الاستجابة:**
```json
{
  "message": "تم تحديث العميل بنجاح"
}
```

---

## المطاعم

### 1. جلب جميع المطاعم
**الطريقة:** `GET`
**المسار:** `/api/restaurants`

**الوصف:** يسترجع قائمة بجميع المطاعم المسجلة في النظام

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/restaurants
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "name": "مطعم البيك",
    "description": "أفضل دجاج مقرمش",
    "image": "https://example.com/albaik.jpg",
    "category": "مطاعم سريعة",
    "address": "شارع الملك فهد، جدة",
    "phone": "0122345678",
    "email": "info@albaik.com",
    "status": "active",
    "rating": 4.8,
    "delivery_fee": 5.0,
    "min_order": 20.0,
    "delivery_time": "30-45 دقيقة",
    "created_at": "2024-01-10 09:00:00",
    "updated_at": "2024-01-10 09:00:00"
  }
]
```

---

### 2. إضافة مطعم جديد
**الطريقة:** `POST`
**المسار:** `/api/restaurants`

**البيانات المطلوبة في الـ Body:**
```json
{
  "name": "مطعم جديد",
  "description": "وصف المطعم",
  "image": "https://example.com/image.jpg",
  "category": "مأكولات شرقية",
  "address": "العنوان الكامل",
  "phone": "0501234567",
  "email": "restaurant@example.com",
  "delivery_fee": 10.0,
  "min_order": 30.0
}
```

**مثال على الاستجابة:**
```json
{
  "id": 5,
  "message": "تم إضافة المطعم بنجاح"
}
```

---

## الطلبات

### 1. جلب جميع الطلبات
**الطريقة:** `GET`
**المسار:** `/api/orders`

**معاملات الاستعلام الاختيارية:**
- `status`: حالة الطلب (pending, confirmed, preparing, out_for_delivery, completed, cancelled)
- `limit`: عدد النتائج المراد عرضها

**أمثلة على الطلبات:**
```bash
# جميع الطلبات
GET http://localhost:3000/api/orders

# الطلبات قيد التحضير فقط
GET http://localhost:3000/api/orders?status=preparing

# أحدث 10 طلبات
GET http://localhost:3000/api/orders?limit=10

# الطلبات المكتملة مع حد أقصى 20
GET http://localhost:3000/api/orders?status=completed&limit=20
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "customer_id": 5,
    "restaurant_id": 2,
    "captain_id": 3,
    "status": "out_for_delivery",
    "total_amount": 85.50,
    "delivery_fee": 10.0,
    "delivery_address": "حي النسيم، شارع الأمير محمد، مبنى 123",
    "customer_notes": "يرجى الاتصال عند الوصول",
    "created_at": "2024-10-03 14:30:00",
    "updated_at": "2024-10-03 14:45:00",
    "customer_name": "أحمد محمد",
    "customer_phone": "0501234567",
    "restaurant_name": "مطعم البيك",
    "captain_name": "خالد السائق"
  }
]
```

---

### 2. تحديث حالة الطلب
**الطريقة:** `PATCH`
**المسار:** `/api/orders/:id/status`

**المعاملات:**
- `id` (في المسار): رقم معرف الطلب

**البيانات المطلوبة في الـ Body:**
```json
{
  "status": "completed"
}
```

**الحالات المتاحة:**
- `pending`: في الانتظار
- `confirmed`: تم التأكيد
- `preparing`: قيد التحضير
- `out_for_delivery`: في الطريق للتوصيل
- `completed`: مكتمل
- `cancelled`: ملغي

**مثال على الاستجابة:**
```json
{
  "message": "تم تحديث حالة الطلب بنجاح"
}
```

---

## الكباتن

### 1. جلب جميع الكباتن
**الطريقة:** `GET`
**المسار:** `/api/captains`

**الوصف:** يسترجع قائمة بجميع الكباتن مع معلومات المستخدم الخاصة بهم

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/captains
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "user_id": 10,
    "vehicle_type": "دراجة نارية",
    "license_number": "ABC-1234",
    "location": "جدة - حي النسيم",
    "status": "available",
    "rating": 4.7,
    "total_orders": 156,
    "created_at": "2024-01-05 08:00:00",
    "updated_at": "2024-10-03 15:00:00",
    "name": "خالد أحمد",
    "email": "khaled@example.com",
    "phone": "0509876543",
    "user_status": "active"
  }
]
```

---

### 2. جلب الكباتن المتاحين
**الطريقة:** `GET`
**المسار:** `/api/captains/available`

**الوصف:** يسترجع فقط الكباتن المتاحين حالياً (status = 'available') والنشطين

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/captains/available
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "user_id": 10,
    "vehicle_type": "دراجة نارية",
    "license_number": "ABC-1234",
    "location": "جدة - حي النسيم",
    "status": "available",
    "rating": 4.7,
    "total_orders": 156,
    "name": "خالد أحمد",
    "phone": "0509876543"
  }
]
```

---

## التقارير والإحصائيات

### 1. إحصائيات لوحة التحكم
**الطريقة:** `GET`
**المسار:** `/api/reports/dashboard`

**الوصف:** يسترجع جميع الإحصائيات الرئيسية للوحة التحكم

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/reports/dashboard
```

**مثال على الاستجابة:**
```json
{
  "totalOrders": 45,
  "activeCustomers": 1250,
  "availableCaptains": 23,
  "totalSales": 12500.50,
  "dailyTarget": 200,
  "averageDeliveryTime": 28
}
```

**شرح البيانات:**
- `totalOrders`: عدد الطلبات اليوم
- `activeCustomers`: عدد العملاء النشطين
- `availableCaptains`: عدد الكباتن المتاحين حالياً
- `totalSales`: إجمالي المبيعات اليوم (الطلبات المكتملة فقط)
- `dailyTarget`: الهدف اليومي للطلبات
- `averageDeliveryTime`: متوسط وقت التوصيل بالدقائق

---

## التسويق

### 1. جلب جميع الإعلانات
**الطريقة:** `GET`
**المسار:** `/api/marketing/ads`

**الوصف:** يسترجع قائمة بجميع الإعلانات مرتبة من الأحدث للأقدم

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/marketing/ads
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "title": "عرض خاص على البيتزا",
    "content": "احصل على خصم 50% على جميع أنواع البيتزا",
    "image": "https://example.com/pizza-offer.jpg",
    "type": "banner",
    "restaurant_id": 3,
    "category": "عروض",
    "status": "active",
    "start_date": "2024-10-01",
    "end_date": "2024-10-31",
    "views": 5420,
    "clicks": 342,
    "created_at": "2024-09-28 10:00:00"
  }
]
```

---

### 2. إضافة إعلان جديد
**الطريقة:** `POST`
**المسار:** `/api/marketing/ads`

**البيانات المطلوبة في الـ Body:**
```json
{
  "title": "عنوان الإعلان",
  "content": "محتوى الإعلان التفصيلي",
  "image": "https://example.com/image.jpg",
  "type": "banner",
  "restaurant_id": 5,
  "category": "عروض",
  "start_date": "2024-11-01",
  "end_date": "2024-11-30"
}
```

**أنواع الإعلانات المتاحة:**
- `banner`: بانر إعلاني
- `popup`: إعلان منبثق
- `notification`: إشعار

**مثال على الاستجابة:**
```json
{
  "id": 8,
  "message": "تم إنشاء الإعلان بنجاح"
}
```

---

### 3. جلب جميع القسائم
**الطريقة:** `GET`
**المسار:** `/api/marketing/coupons`

**الوصف:** يسترجع قائمة بجميع قسائم الخصم

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/marketing/coupons
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "code": "RAMADAN2024",
    "discount_type": "percentage",
    "discount_value": 20,
    "min_order": 50.0,
    "usage_limit": 100,
    "used_count": 45,
    "expiry_date": "2024-04-30",
    "status": "active",
    "created_at": "2024-03-01 08:00:00"
  }
]
```

**أنواع الخصم:**
- `percentage`: نسبة مئوية
- `fixed`: مبلغ ثابت

---

### 4. إضافة قسيمة جديدة
**الطريقة:** `POST`
**المسار:** `/api/marketing/coupons`

**البيانات المطلوبة في الـ Body:**
```json
{
  "code": "SUMMER50",
  "discount_type": "percentage",
  "discount_value": 50,
  "min_order": 100.0,
  "usage_limit": 500,
  "expiry_date": "2024-09-30"
}
```

**مثال على الاستجابة:**
```json
{
  "id": 12,
  "message": "تم إنشاء القسيمة بنجاح"
}
```

---

## نظام الخطوات

### 1. إضافة سجل خطوات جديد
**الطريقة:** `POST`
**المسار:** `/api/steps`

**البيانات المطلوبة في الـ Body:**
```json
{
  "userId": "user_123",
  "steps": 8500,
  "date": 1696320000
}
```

**ملاحظة:** التاريخ يجب أن يكون بصيغة Unix Timestamp

**مثال على الاستجابة:**
```json
{
  "success": true,
  "id": 45
}
```

**في حالة البيانات الناقصة:**
```json
{
  "error": "بيانات ناقصة"
}
```

---

### 2. جلب خطوات مستخدم محدد
**الطريقة:** `GET`
**المسار:** `/api/steps/:userId`

**المعاملات:**
- `userId` (في المسار): معرف المستخدم

**مثال على الطلب:**
```bash
GET http://localhost:3000/api/steps/user_123
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "userId": "user_123",
    "steps": 8500,
    "date": 1696320000
  },
  {
    "id": 2,
    "userId": "user_123",
    "steps": 10200,
    "date": 1696233600
  }
]
```

---

### 3. تحديث عدد الخطوات
**الطريقة:** `PUT`
**المسار:** `/api/steps/:id`

**المعاملات:**
- `id` (في المسار): رقم معرف السجل

**البيانات المطلوبة في الـ Body:**
```json
{
  "steps": 12000
}
```

**مثال على الاستجابة:**
```json
{
  "success": true,
  "changes": 1
}
```

---

### 4. حذف سجل خطوات
**الطريقة:** `DELETE`
**المسار:** `/api/steps/:id`

**المعاملات:**
- `id` (في المسار): رقم معرف السجل

**مثال على الطلب:**
```bash
DELETE http://localhost:3000/api/steps/5
```

**مثال على الاستجابة:**
```json
{
  "success": true,
  "changes": 1
}
```

---

## الأوامر

### 1. إضافة أمر جديد
**الطريقة:** `POST`
**المسار:** `/commands`

**البيانات المطلوبة في الـ Body:**
```json
{
  "name": "اسم الأمر",
  "value": "قيمة الأمر"
}
```

**مثال على الاستجابة:**
```json
{
  "id": 7,
  "name": "اسم الأمر",
  "value": "قيمة الأمر"
}
```

---

### 2. جلب جميع الأوامر
**الطريقة:** `GET`
**المسار:** `/commands`

**مثال على الطلب:**
```bash
GET http://localhost:3000/commands
```

**مثال على الاستجابة:**
```json
[
  {
    "id": 1,
    "name": "أمر تجريبي",
    "value": "قيمة تجريبية"
  }
]
```

---

### 3. حذف أمر
**الطريقة:** `DELETE`
**المسار:** `/commands/:id`

**المعاملات:**
- `id` (في المسار): رقم معرف الأمر

**مثال على الطلب:**
```bash
DELETE http://localhost:3000/commands/3
```

**مثال على الاستجابة:**
```json
{
  "deletedID": "3"
}
```

---

## رموز الاستجابة (HTTP Status Codes)

| الكود | الوصف |
|------|--------|
| 200 | نجحت العملية بشكل طبيعي |
| 400 | خطأ في البيانات المرسلة أو طلب غير صالح |
| 500 | خطأ في السيرفر |

---

## معلومات إضافية

### قاعدة البيانات
المشروع يستخدم **SQLite** كقاعدة بيانات محلية في الملف `database.db`

### الجداول الرئيسية:
- `users` - جدول المستخدمين
- `restaurants` - جدول المطاعم
- `products` - جدول المنتجات
- `orders` - جدول الطلبات
- `captains` - جدول الكباتن
- `ads` - جدول الإعلانات
- `coupons` - جدول القسائم
- `steps` - جدول الخطوات

### CORS
السيرفر مفعّل عليه CORS، مما يسمح بالطلبات من أي نطاق (domain)

### المنفذ (Port)
السيرفر يعمل على المنفذ `3000` بشكل افتراضي

---

## أمثلة على الاستخدام باستخدام JavaScript

### مثال 1: جلب جميع الطلبات
```javascript
fetch('http://localhost:3000/api/orders')
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error('خطأ:', error));
```

### مثال 2: إضافة مطعم جديد
```javascript
const newRestaurant = {
  name: "مطعم السلطان",
  description: "أشهى المأكولات الشرقية",
  image: "https://example.com/sultan.jpg",
  category: "مأكولات شرقية",
  address: "الرياض، حي العليا",
  phone: "0112345678",
  email: "info@sultan.com",
  delivery_fee: 15.0,
  min_order: 40.0
};

fetch('http://localhost:3000/api/restaurants', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(newRestaurant)
})
  .then(response => response.json())
  .then(data => console.log('تم إضافة المطعم:', data))
  .catch(error => console.error('خطأ:', error));
```

### مثال 3: تحديث حالة طلب
```javascript
const orderId = 5;
const newStatus = { status: 'completed' };

fetch(`http://localhost:3000/api/orders/${orderId}/status`, {
  method: 'PATCH',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify(newStatus)
})
  .then(response => response.json())
  .then(data => console.log('تم تحديث الطلب:', data))
  .catch(error => console.error('خطأ:', error));
```

---

## الملاحظات الهامة

1. **التاريخ والوقت**: جميع التواريخ في قاعدة البيانات تستخدم التوقيت المحلي للسيرفر
2. **الحقول المطلوبة**: تأكد من إرسال جميع الحقول المطلوبة في طلبات POST و PUT
3. **حالة الطلبات**: عند تحديث حالة الطلب، تأكد من استخدام إحدى الحالات المحددة فقط
4. **معرفات المستخدمين**: في نظام الخطوات، `userId` يمكن أن يكون نص (string) وليس بالضرورة رقم

---

## الدعم الفني
في حالة وجود أي مشاكل أو استفسارات، يرجى التواصل مع فريق التطوير.

**تاريخ آخر تحديث:** 3 أكتوبر 2025
